---
name: security
description: Load when reviewing or writing code that handles authentication, authorization, secrets, user input, or anything that touches the network boundary. Forces the author to enumerate threats before implementing.
---

# Security

Use when the change touches any of: authentication, authorization,
sessions, secrets, env vars, request bodies, URLs, CORS, headers, file
uploads, SQL/ORM calls, dependency installation, Dockerfiles, or any
infrastructure configuration.

Full skill body lives in **`.agents/skills/security.md`**.