{
  "name": "@baseplate/e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Playwright end-to-end suite. Runs against the compose.prod stack.",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "clean": "rm -rf playwright-report test-results"
  },
  "devDependencies": {
    "@baseplate/contracts": "workspace:*",
    "@playwright/test": "^1.49.1",
    "@types/node": "^22.10.2"
  }
}
